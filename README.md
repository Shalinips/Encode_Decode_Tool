Encode Decode Tool 🔐

A simple Python GUI application that allows users to encode and decode text messages using a numeric key.  
This project was developed as a beginner-level mini project using Python and Tkinter.


📌 Features
- Encode messages using a numeric key
- Decode messages using the same key
- User-friendly graphical interface
- Error handling for invalid input
- Background image support


🛠️ Technologies Used
- Python
- Tkinter (GUI)
- Pillow (for image handling)


⚙️ How It Works
The application uses a basic character-shifting technique:
- Each character in the message is converted to its ASCII value
- The numeric key is added (encoding) or subtracted (decoding)
- The transformed characters form the encoded or decoded message


▶️ How to Run the Project

1. Clone the repository:
   ```bash
   git clone https://github.com/Shalinips/Encode_Decode_Tool.git

2.Navigate to the project folder

3.Install required dependencies: pip install -r requirements.txt

4.Run the application: python encode_decode.py


👩‍💻 Author(Shalini P S)
